{
  "name": "aether-rwa-subgraph",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ aether-rwa",
    "create-local": "graph create --node http://localhost:8020/ aether-rwa",
    "remove-local": "graph remove --node http://localhost:8020/ aether-rwa",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 aether-rwa",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.98.0",
    "@graphprotocol/graph-ts": "^0.38.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
